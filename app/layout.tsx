import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'ATLAS | America\'s Most Popular Pergola Brand',
  description: 'Transform your outdoor space with Scandinavian-inspired aluminum pergolas. Engineered for American weather, designed for modern living.',
  keywords: 'pergola, aluminum pergola, outdoor living, patio cover, backyard, DIY pergola',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
